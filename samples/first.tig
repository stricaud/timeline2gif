# The simplest possible timeline — just events.
# Everything else (canvas, theme, fonts, spacing, speed) uses sensible
# built-in defaults. Add settings only when you want to override them;
# see docs/syntax.md for the full list.

"Jan 2020" "My story begins!"
"Feb 2020" "We all went to RSA"
"Mar 2020" "FIRST postponed, CanSec went online"
"Jun 2020" "Remote work becomes the norm"
